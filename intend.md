# intend — 이 저장소를 만드는 이유

> 이 문서는 "무엇을 만드는가"가 아니라 "왜 만드는가"를 적는다.
> 구현 방법은 `docs/ARCHITECTURE.md`, 결정의 근거는 `docs/ADR.md`에 있다.

---

## 1. 최종 목적

이 저장소는 목적이 아니라 수단이다.

나중에 multi-modal LLM agent가 protein-ligand 복합체를 보고 "다음에 이 화학구조를 이렇게 바꿔라"를 제안하게 될 것이다. 그때 그 제안이 근거 없는 그럴듯한 말이 되지 않도록, **전례를 공급하는 것**이 이 저장소의 일이다.

그래서 이 저장소가 답해야 할 질문은 하나로 압축된다.

> 지금 보고 있는 이 포켓의 이 위치에서, 이런 변형을 시도한 전례가 있는가.
> 있었다면 무슨 일이 일어났고, 대가는 무엇이었는가.

이 질문에 답할 수 있으면 성공이고, 답하지 못하면 문헌이 몇 편 쌓였는지는 의미가 없다.

## 2. 두 번째 목적

같은 구조의 지식베이스를 다른 도메인에 대해 쉽게 복제할 수 있어야 한다.

그래서 이 저장소는 두 부분으로 갈라져 있다. `src/kbase/`는 지식의 내용을 모르는 기계류다. `src/domains/kinase/`는 kinase를 아는 유일한 곳이다. 두 번째 도메인은 `domains/` 아래 폴더 하나를 새로 채우는 일이 되어야 한다.

이 경계는 말로 지키는 것이 아니라 테스트로 지킨다. `tests/test_engine_isolation.py`가 엔진이 도메인을 import하는지 감시한다.

## 3. 무엇이 아닌가

명시적으로 하지 않는 일을 적어 둔다. 범위가 흐려지면 전부 흐려진다.

**논문 데이터베이스가 아니다.** PubMed를 복제하는 것이 목적이 아니다. 논문은 근거의 출처일 뿐이고, 중심 객체는 화합물 변형 한 건(Design Move)이다.

**구조 해석 엔진이 아니다.** 복합체를 받아 접촉을 계산하고 포켓을 판정하는 일은 소비자 agent가 한다. 이 저장소는 그 결과로 만들어진 descriptor를 받아 전례를 돌려준다. 경계를 흐리면 이 저장소가 구조 분석 도구로 변질된다.

**검증된 지식의 저장소가 아니다.** 사람이 검수하지 않는다. 모든 레코드는 미검수이고, 기계 검사를 통과했다는 사실만 주장한다. 이 한계를 숨기지 않고 `dist/CONTRACT.md`에 적어 소비자에게 전달한다.

**설계 규칙 교과서가 아니다.** "언제 이 전술을 쓰고 언제 피하라"는 산문형 일반화를 만들지 않는다. 검수하는 사람이 없는 일반화는 오류가 개별 레코드보다 훨씬 멀리 퍼진다. 대신 태그 조합별 집계를 제공한다. "이 부위 × 이 목표 × 이 전술의 전례 7건, 개선 5건, 악화 2건"은 세어본 결과이므로 틀릴 수가 없다.

**MVP가 아니다.** 기능을 줄여 빨리 출시하는 것이 목표가 아니다. 추출된 정보가 정확히 관리되는 것이 목표다.

## 4. 왜 Design Move가 중심인가

논문, 사례(case), 규칙(rule), 구조 부위(concept) 중 무엇을 원자 단위로 둘지가 이 설계의 첫 갈림길이었다. Design Move를 골랐다.

소비자 agent가 실제로 필요한 것은 "이 포켓에 이 치환기를 넣었더니 이렇게 됐다"는 한 건의 전례다. Move는 구조 관찰, 화학적 변경, 측정된 효과, 대가, 근거 위치를 한 덩어리로 묶는다. 그래서 **검색 단위와 인용 단위가 일치한다.**

Case를 중심에 두면 검색 단위가 너무 커서 "지금 이 포켓"에 해당하는 조각만 뽑아낼 수 없다. Rule을 중심에 두면 사람이 써야 하는데 검수자가 없다. 구조 부위를 중심에 두면 물성이나 selectivity처럼 부위로 안 떨어지는 지식이 갈 곳을 잃는다.

## 5. 왜 수치를 까다롭게 다루는가

이 저장소의 실패 방식 중 가장 위험한 것은 정보 부족이 아니라 **그럴듯한 오류**다.

논문에 IC50이 `>10000`에서 `11`로 개선됐다고 나올 때, 이를 "909배 개선"으로 저장하면 하한값이 정확한 값으로 둔갑한다. 하류 agent는 이 909라는 숫자를 신뢰하고 우선순위를 정할 것이다. 원문에는 없던 정밀도가 파이프라인 중간에서 태어난 것이다.

그래서 세 가지를 규칙으로 못 박았다.

1. 부등호가 있으면 배수를 계산하지 않는다.
2. assay 조건이 같다고 확인되지 않으면 배수를 계산하지 않는다.
3. 그 대신 개선/악화 방향과 대략의 크기 등급은 수치가 없어도 항상 채운다.

3번이 중요하다. 1번과 2번만 있으면 조건을 명시하지 않은 논문의 사례가 전부 버려진다. 정성적으로는 쓸모 있는 전례를 버리게 된다.

## 6. 사람 검수가 없다는 선택의 의미

상시 검수를 하지 않기로 했다. 이 선택은 되돌릴 수 있지만, 되돌리기 전까지는 정직하게 살아야 한다.

그래서 `validated`, `confidence: high` 같은 필드를 아예 만들지 않는다. 필드가 있으면 언젠가 누군가 채우고, 채워진 값은 근거 없이 신뢰를 얻는다.

대신 검증의 무게를 기계에 모두 실었다.

- MinerU와 Docling 두 파서가 같은 수치를 뽑았는지 대조한다.
- 인용한 수치를 ChEMBL의 같은 문서 activity와 대조한다. 이건 PDF를 보지 않은 독립적인 증거다.
- quote가 파싱 원문에 실제로 존재하는지 확인한다.
- PMID, PDB, 구조 참조의 실재를 확인한다.

이 세 층이 무엇을 못 잡는지도 적어 둔다. 두 파서가 같은 실수를 하면 통과한다. 숫자가 맞아도 다른 화합물의 값일 수 있다. 관찰에서 효과로 이어지는 인과 해석의 타당성은 어느 검사도 보증하지 않는다.

## 7. 왜 소스가 엇갈릴 때 고르지 않는가

gatekeeper 잔기 번호처럼 단순해 보이는 값도 소스마다 다를 수 있다. KLIFS와 Kincore가 같은 구조의 DFG 상태를 다르게 판정할 수도 있다.

검수하는 사람이 있으면 "이 경우는 KLIFS가 맞다"고 판단할 수 있다. 검수자가 없으면 그 판단을 검토할 사람도 없다. 그래서 고르지 않고 양쪽 원값과 판정 기준을 보존한 채 `conflict`로 남긴다. 충돌을 소비자까지 전달하고, 소비자가 자기 맥락에서 판단하게 한다.

이건 게으름이 아니라 정보 보존이다. 임의로 하나를 고르면 그 순간 다른 쪽 정보가 영구히 사라진다.

## 8. 성공 판정 기준

### 첫 단계 (논문 3편)

1. 논문 3편에서 Move가 생성되고 기계 검사 9종이 전부 기록된다.
2. 각 Move의 관찰, 변형, 효과를 원문의 페이지와 표 위치로 되짚을 수 있다.
3. 예제 질의 5개가 관련 Move와 근거를 돌려준다. 결과가 없어야 정상인 질의와 `unknown`을 포함하는 부정 질의를 반드시 넣는다.
4. KLIFS와 Kincore가 엇갈리는 구조를 하나 골라, `conflict`가 소비자까지 전달되는지 확인한다.
5. 같은 입력으로 재실행하면 `kb/`와 `dist/`가 바이트 단위로 동일하다.
6. 잘못된 PDF 하나가 기존 KB를 손상시키지 않는다.

### 두 번째 단계 (논문 10편 이상)

7. 태그 조합별 집계가 의미 있는 숫자를 갖는다. 전례 1건짜리 조합만 나열되면 실패다.
8. 전체 kinome reference가 채워져 있고, 처음 보는 kinase에 대해서도 좌표를 돌려준다. 모르는 것은 `unmapped`로 정직하게 말한다.

### 궁극적 기준

소비자 agent에게 이 저장소를 붙였을 때, 붙이기 전보다 제안의 근거가 좋아져야 한다. 이건 자동으로 측정되지 않는다. 실제 복합체를 몇 개 넣어보고 사람이 판단할 일이다.

## 9. 실패를 알아차리는 신호

다음이 보이면 설계가 잘못 가고 있다.

- Move는 쌓이는데 태그 조합별 집계가 전부 1건짜리다. 어휘가 너무 세분화됐거나 문헌 수집이 흩어졌다.
- `moves_flagged.jsonl`이 `moves.jsonl`보다 크다. 추출 프롬프트나 파싱이 근본적으로 안 맞는다.
- `proposed_tags`가 계속 쌓이는데 승격되지 않는다. 폐쇄 어휘가 현실과 안 맞는다.
- 소비자 질의가 항상 같은 Move 몇 개만 돌려준다. 태그가 실질적인 구분을 못 하고 있다.
- 엔진 코드에 kinase 용어가 새어 들어온다. 템플릿 목적이 무너지는 시작이다.

## 10. 관련 문서

| 문서 | 내용 |
|---|---|
| `AGENTS.md` | 이 저장소에서 작업할 때의 규칙 (`CLAUDE.md`가 import) |
| `docs/consumer/AGENTS.md` | 이 지식을 소비하는 agent를 위한 안내 (영어) |
| `docs/PRD.md` | 사용자, 기능 범위, 하지 않을 일 |
| `docs/ARCHITECTURE.md` | 디렉터리 구조, 데이터 흐름, 스키마 |
| `docs/ADR.md` | 설계 결정 27건과 각각의 트레이드오프 |
| `dist/CONTRACT.md` | 빌드가 생성. 소비 계약과 신뢰 한계 (영어) |
