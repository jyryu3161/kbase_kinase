# plan.md 리뷰

> 검토일: 2026-09-15 · 대상: 현재 `plan.md` (662행)
> 이 문서는 이전 명세에 대한 잠정 리뷰를 대체한다. `plan.md` 원문은 수정하지 않았다.
> 판정: **핵심 방향은 유지하되, 자동 승격·매핑·근거 검증 계약을 고친 뒤 구현하는 것을 권장한다.**

## 1. 전체적인 아이디어 — 8줄

1. Design Move를 중심으로 구조 관찰, 화학적 변경, 측정 효과, trade-off, 근거를 연결하는 방향은 목적에 잘 맞는다.
2. SITE·GOAL·TACTIC을 나눈 것은 구조 중심 사례와 물성 중심 사례를 함께 검색하기에 적절하다.
3. `kb/`의 큐레이션 지식, `ref/`의 외부 자료, `dist/`의 소비용 산출물을 분리하는 설계도 유지할 가치가 있다.
4. KB의 역할은 관련 전례와 적용 조건을 공급하는 것이고, 새로운 복합체의 해석과 변형 제안은 소비자 agent가 맡는다.
5. 가장 큰 문제는 숫자·ID가 맞는다는 자동 검사를 주장 내용이 검증됐다는 의미로 확대하는 부분이다.
6. KLIFS 위치 매핑과 구조 상태의 출처 연결도 단순 일치 검사만으로 정확성을 보장하기 어려워 실패·불확실성을 표현해야 한다.
7. 파일 기반 계약은 유지하되, 범용 매퍼·가중 유사도·다중 언어 리더·전체 수집 자동화를 첫 버전에 모두 넣을 필요는 없다.
8. 먼저 검수된 Move 5개와 연결 그림으로 조회→근거 확인까지 완성한 뒤, 반복 작업을 자동화하는 순서를 권장한다.

## 2. 아키텍처·지식베이스 역할과 기능 리뷰

### 2.1 구현 전에 고칠 핵심 사항

#### P1-1. 자동 검사와 사람 검수가 같은 신뢰도 체계에 섞여 있다

**위치:** `plan.md` 143–158, 426, 442–443, 526행.

`kb/`는 사람이 보증한다고 정의하지만 기계 검사 통과분을 자동 승격한다. PMID·PDB의 실재와 숫자의 출현은 관찰→변형→효과의 연결이 맞다는 증거가 아니다. 한편 미검토 자료를 소비하게 하면서 `check-refs`는 `unreviewed`를 실패 처리한다. 기본 리더에는 status 필터도 명시되어 있지 않다.

**수정 제안:**

- 검수는 `review_status: pending | validated | rejected`, 자동 검사는 `checks_passed`로 분리한다. 자동 검사 통과를 `confidence: medium`으로 변환하지 않는다.
- `kb/`에는 사람 승인분을 두고, 자동 검사 통과 후보는 staging에 유지한다.
- 미검토 자료도 배포한다면 `candidates.jsonl`로 분리한다. 기본 `get/find/similar/figures/cite`는 검수된 자료만 사용하고, 후보 탐색은 명시적으로 켠다.
- 후보를 인용하는 탐색 모드를 허용할 경우 `cite`와 `check-refs`가 동일한 정책을 적용하도록 한다. 순위 할인만으로 검수 여부를 대신하지 않는다.
- 사람의 작업량은 테마별 리뷰·우선순위·중복 억제로 줄인다. 승격 기준을 느슨하게 만드는 것과 분리한다.

#### P1-2. 숫자 존재 검사로 SAR의 정확성을 보장할 수 없다

**위치:** 179–187, 202–207, 443, 472–479행.

원문에 `11`이 있어도 다른 화합물이나 다른 assay의 값일 수 있다. LLM이 읽은 파싱 결과에서 다시 숫자를 찾으면 파서가 이미 잃어버린 수치는 감지하지 못한다. 예시의 `before >10000`, `after =11`을 `fold_change: 909`로 저장하면 하한이 정확한 값으로 바뀐다. 표 각주를 버리면 assay 조건과 부등호 의미도 유실될 수 있다.

**수정 제안:**

- 효과를 `(paper, table/figure, row/column 또는 bbox, compound, target/variant, assay, metric, value, unit, qualifier)`에 연결한다.
- 같은 조건인지 확인되지 않은 before/after에는 개선 배수를 계산하지 않는다.
- 위 예시는 정확한 909배 대신 하한임을 보존한다. 최소 구현에서는 부등호가 있는 값의 배수 계산을 보류해도 된다.
- `verified_against: text`를 내용 검수 완료로 해석하지 않는다. 자동 span 검사와 원문 대조 검수를 나눈다.
- 원본 PDF의 해당 페이지·표·각주를 재확인할 수 있게 유지한다. 파싱 누락 의심 시 별도 텍스트 추출이나 OCR 결과와 대조한다.
- observation, effect, trade-off마다 근거를 연결한다. Figure 3의 접촉 설명 하나가 효능 수치나 임상적 trade-off까지 뒷받침한다고 취급하지 않는다.

#### P1-3. 85자 pocket 서열 정렬만으로 범용 위치 매퍼를 만들겠다는 설명이 부족하다

**위치:** 55–81, 513, 638행.

85개 pocket 위치에 라벨을 붙이는 일과 입력의 전체 서열·구조에서 그 위치를 정확히 찾는 일은 다르다. KLIFS는 구조 정렬과 pocket 위치에 최적화된 master alignment를 사용한다. 따라서 짧은 pocket 문자열과 입력의 단순 정렬만으로 범용 매퍼가 검증됐다고 볼 수 없다. [KLIFS FAQ](https://klifs.net/faq.php), [KLIFS 원 논문](https://pmc.ncbi.nlm.nih.gov/articles/PMC4702798/)

**수정 제안:**

- 첫 버전은 지원 target을 한정하고, 검수된 `KLIFS position ↔ reference sequence position` 표와 입력→reference 매핑을 조합한다.
- reference의 accession·isoform·서열 버전, 구조의 chain·residue 번호 체계·insertion code를 보존한다.
- WT가 기대 위치에 있는지 확인하는 검사와, 변이 구조에 예상 mutant residue가 있는지 확인하는 검사를 구분한다. T315I 구조에서 WT T만 요구하면 정상 변이를 거부한다.
- 아미노산 하나의 일치는 필요 검사일 수 있지만 올바른 위치의 충분조건은 아니다. 주변 정렬, 도메인, 여러 앵커를 함께 확인한다.
- 갭은 `not_observed`, `not_applicable`, `unmapped` 등 원인을 구분한다. 갭을 곧바로 생물학적 위치 부재로 단정하지 않는다.
- 지원 밖의 target이나 모호한 정렬은 `unmapped`로 반환한다. 범용 85위치 매퍼는 별도 확장 단계로 둔다.

#### P1-4. PDB ID만으로 그림에 상태를 상속하면 다른 복합체의 상태가 붙을 수 있다

**위치:** 198, 249–255, 291–295, 312–314, 560–574행.

한 그림은 여러 패널과 여러 구조를 담을 수 있다. `PDB-3OXZ` 같은 ID 하나만으로는 어느 chain·ligand·패널을 뜻하는지 부족하다. 캡션의 정규식 일치도 부정문이나 비교 대상을 구분하지 못한다. `inherited=최고`, `lexical=높음`, `human=확정`처럼 출처를 곧바로 진실성 등급으로 만드는 것은 과도하다.

또한 앞부분은 상태의 1차 소스를 Kincore로 바꾸지만, Figure와 CI 규칙은 여전히 KLIFS 상속값을 정답으로 간주한다. Kincore의 αC 관련 분류에는 구체적인 거리 기준과 결측 상태가 있으므로, 다른 소스의 필드와 같은 의미인지 정의해야 한다. [Kincore 공식 분류 설명](https://dunbrack.fccc.edu/kincore/)

**수정 제안:**

- 구조 참조를 최소 `pdb_id + chain + ligand 식별자`로 만들고, 필요한 경우 model/altloc을 추가한다.
- 그림 상태는 정확히 연결된 패널에만 붙인다. 복합 그림의 모든 상태를 하나로 합치지 않는다.
- 필드마다 `source`, `source_record`, `source_version`, `method`를 둔다. provenance 종류와 검수 상태를 분리한다.
- Kincore와 KLIFS가 충돌하면 원값과 기준을 보존하고 `conflict`로 표시한다. 사람이 수정한 값이 있다는 이유만으로 CI가 기계값을 강제하지 않게 한다.
- 사내 구조나 DB에 없는 구조는 위치 매핑만으로 DFG·αC 상태가 생기지 않는다. 소비자가 제공한 검증 가능한 descriptor 또는 `unknown`으로 처리한다.

#### P1-5. 내용 해시 ID가 안정적인 인용 ID 역할까지 맡고 있다

**위치:** 163, 217, 395, 414, 446행.

관찰이나 변형의 문구를 고치면 ID가 바뀐다. LLM 재추출의 표현 차이로도 신규 Move가 생기고, 연결된 mutation·figure·인용이 끊길 수 있다. 반대로 같은 ID의 수치·근거가 바뀔 수 있으므로 레코드 ID만으로 해석 버전이 고정되지도 않는다. 거부 원장 역시 문구가 바뀌면 동일 후보를 놓친다.

**수정 제안:** 최초 승인 시 고정 ID를 부여하고 이후 유지한다. `content_hash`는 변경 감지 전용으로 두며 인용에는 `kb_version` 또는 record revision을 포함한다. 후보 중복은 논문·표/그림 위치·화합물 쌍을 이용해 탐지한 뒤 검수한다. 별도 근사 중복 엔진은 당장 만들지 않는다.

### 2.2 기능 계약에서 추가로 고칠 부분

| 우선순위 | 위치 | 문제 | 단순한 수정 |
|---|---|---|---|
| P1 | 353 vs 393행 | 부정은 명시 상태가 있어야 한다면서 jq 예시는 covalent 태그가 없는 레코드를 모두 비공유결합으로 취급한다. | `is_covalent == false`만 선택하도록 모든 예제를 통일한다. `unknown`은 제외한다. |
| P1 | 426행 | ID 존재·quote 일치만으로 제안이 근거에서 도출되는지까지 검사할 수는 없다. | `check-refs`는 참조 무결성 검사라고 범위를 명시한다. 주장별 evidence 연결과 내용 평가는 별도로 둔다. |
| P2 | 237, 422–428행 | 같은 KLIFS 위치·기전이라는 연결이 타 target에서의 효과까지 보장하지 않는다. | `analogous_mutations`는 유사 전례 후보로 반환한다. 전이는 `hypothesis`로 표시하고 자동 `transfer_confidence: medium`을 부여하지 않는다. |
| P2 | 125, 341, 364–371행 | subpocket의 true가 존재·열림·점유 중 무엇인지 불명확하고 유사도에서 unknown 처리도 없다. | 첫 버전은 `occupied: true/false/null`처럼 하나의 의미만 정의한다. unknown끼리의 일치는 유사도에 가점하지 않는다. |
| P2 | 395행 | Move ID별 데이터 분할이면 같은 논문·화합물 계열의 Move가 학습/평가 양쪽에 들어갈 수 있다. | 최소 paper 단위로 분할하고, 연구 평가 목적에 따라 series 단위까지 묶는다. |
| P2 | 153–158, 552행 | `ref/`가 외부 최신값으로만 재생성되면 과거 dist 재현이 안 된다. 캐시도 gitignore다. | 사용한 snapshot의 식별자·해시·보관 경로를 manifest에 고정한다. 갱신과 build를 분리한다. |
| P2 | 129–131, 193–196행 | 3축 어휘라고 하지만 예시에는 mutation_class·gk 등이 추가되고 raw 필드는 sites만 있다. | 태그는 SITE/GOAL/TACTIC으로 시작하고, 나머지는 typed descriptor로 둔다. 미정 어휘가 curated에 들어갈 때의 처리도 정한다. |
| P2 | 651–652행 | 환경변수 검사 실패 시 `assert ..., v`가 전체 dotenv 값을 오류에 출력한다. | 실패 메시지는 키 이름과 누락·형식 오류만 출력한다. 리뷰 과정에서는 `.env`를 열지 않았다. |

### 2.3 권장 역할 분담과 최소 데이터 흐름

```text
외부 문헌·구조 자료 → 수집 원본 / 고정 ref snapshot
                            ↓ 추출·자동 검사
                         staging 후보
                            ↓ 사람 검수
             kb: Move / Mutation / Figure
                            ↓ 오프라인 검증·빌드
                  dist: 파일 + 소비 계약
                            ↓
              소비자 agent가 검색·가설 생성
```

- **Move:** 어떤 화합물을 어떻게 바꾸었고, 어느 실험에서 어떤 결과가 있었는지 담는다. 적용 조건과 비교 불가능한 부분도 함께 반환한다.
- **Mutation:** 특정 약물·target 맥락의 내성 근거와 검수된 위치 주석을 제공한다. 결합 모드 전체에 대한 일반화는 별도 근거가 있어야 한다.
- **Figure:** 원문 그림·표·패널과 Move의 근거를 연결한다. 이미지를 읽은 판단과 DB에서 가져온 맥락을 구분한다.
- **Ref:** 외부 출처의 버전이 고정된 조회 자료다. 검수자가 직접 수정하지 않는다. 320행의 “ref도 사람이 편집” 문구는 앞의 원칙에 맞춰 고친다.
- **Dist/reader:** 검수 상태를 보존하면서 조회·근거 접근·인용을 제공한다. 첫 버전의 정렬은 고정된 단순 규칙으로 충분하다.
- **소비자 agent:** 복합체를 해석해 descriptor를 만들고, KB의 전례를 바탕으로 새로운 가설을 생성한다. KB 검색 성공을 가설의 검증으로 해석하지 않는다.

## 3. 에러 핸들링·검증 플랜

현재 CI 게이트는 지식 오류에 집중되어 있고, 외부 장애·중간 실패·재개·기존 산출물 보존은 덜 구체적이다. 아래 정도면 별도 작업 큐 없이 구현할 수 있다.

| 상황 | 처리 | 기존 데이터와 재개 |
|---|---|---|
| timeout·429·일시적 5xx | 제한시간, 최대 3회 재시도, 서버 대기 지시 반영 | 실패 ID만 기록해 재실행 |
| 인증·설정 오류 | 해당 단계 중단, 비밀값 없는 오류 메시지 | 설정 수정 후 같은 입력으로 재개 |
| 원문 없음 | `not_accessible` 기록 | 메타데이터는 유지하고 전문 추출을 보류 |
| PDF 손상·MinerU 실패 | 해당 문헌 격리, 원본과 오류 보존 | 다른 문헌은 진행. 무한 OCR 재시도 금지 |
| 수치·화합물·표 연결 불명확 | `needs_review`로 남김 | 자동 승인하지 않고 원문 대조 |
| 위치 매핑 모호·소스 상태 충돌 | 해당 필드에 unknown/conflict와 사유 | 후보는 보존, 확정 주장으로 배포하지 않음 |
| 외부 API 장애 중 PDB 검증 | `lookup_failed`와 `not_found` 구분 | 장애 때문에 기존 PDB 링크를 삭제하지 않음 |
| LLM 형식 불일치 | 로컬 검증 후 보정 요청 최대 1회 | 계속 실패하면 후보 격리 |
| dist 생성·검증 실패 | 임시 디렉터리에서 실패 종료 | 마지막 정상 dist 유지 |
| 과도하게 넓은 질의 | total·truncated·추가 필터 안내 반환 | 정상적인 검색을 시스템 오류로 취급하지 않음 |
| 알 수 없는 ID·스키마 비호환 | 명시적 오류 | 0건 검색 결과와 구분 |

### 3.1 재현성과 중복 방지

- 캐시 키는 단계별로 정한다. 입력 해시 외에 parser 버전·옵션, ontology/schema 버전, ref snapshot, 관련 코드 버전을 포함한다. 모델·프롬프트만으로는 전체 파이프라인의 변경을 감지할 수 없다.
- 성공 산출물이 온전할 때만 캐시를 사용한다. 실패·중단 실행을 완료로 기록하지 않는다.
- `sync-ref`는 네트워크를 사용하고 `build --check`는 고정된 로컬 자료만 사용한다. CI가 외부 서비스의 가용성과 최신값에 좌우되지 않게 한다.
- 단일 writer로 시작하고 동시 실행은 잠금 파일로 막는다. 입력 변경 없이 재실행했을 때 curated와 dist가 동일해야 한다.
- manifest에 파일별 해시를 둔다. 레코드 개수만 같아도 내용 변경이나 중복 치환은 발생할 수 있다.
- 자동 계산한 `analogous_mutations`, context 등은 dist에만 쓴다. 사람이 보증한 `kb/`를 빌드가 수정하지 않는다.

### 3.2 우선 필요한 테스트

1. **신뢰도 경계:** 자동 검사 통과 후보가 기본 조회·인용에 검수 지식으로 나오지 않는다.
2. **수치 연결:** 다른 행의 같은 숫자, 부등호, 단위 차이, 서로 다른 assay를 정상 비교로 승인하지 않는다.
3. **매핑:** 지원 target의 WT·mutant·번호 오프셋·결측·모호한 정렬을 구분한다.
4. **그림 연결:** 서로 다른 구조를 가진 두 패널의 상태를 합치지 않는다.
5. **재실행:** timeout 후 재개·내용 수정 후 재검수·빌드 실패 시 기존 dist 보존을 확인한다.
6. **소비:** 근거가 있는 질문·없는 질문·unknown을 포함하는 부정 질의·미지 ID를 확인한다.

그림 100건 골드셋과 정밀도·재현율 CI 게이트는 자동 선별을 실제로 도입할 때 추가한다. 그전에는 선택한 PDF의 그림·표를 사람이 확인하는 것이 더 직접적이다. 성공 기준에 pytest뿐 아니라 **사람이 원문을 대조한 Move와 소비자 질문 결과**도 넣는다.

### 3.3 계획에 적힌 외부 검증 결과의 취급

- EGFR 표본만으로 KLIFS 전체가 20개월 멈췄다고 단정하지 않는다. 쿼리·실행일·중복 제거 기준·응답 snapshot을 보존하고 “해당 표본에서 관측한 커버리지”로 서술한다.
- PMC에서 44편 중 1편을 찾았다는 결과는 모든 전문 경로에서 43편을 자동 확보할 수 없다는 뜻은 아니다. 첫 버전의 수동 PDF 확보 방침은 유지해도 된다.
- 테마별 PubMed 합계는 중복 PMID를 제거한 코퍼스 크기와 구분한다. 380편을 곧바로 검수 작업량으로 잡지 않는다.
- MinerU #5330은 CNKI PDF의 본문 숫자 유실에 대한 사용자 보고다. 해당 보고에서는 표 숫자가 보존되었다. kinase 코퍼스의 실제 실패율이나 사용하려는 로컬 버전의 결과로 확대하지 말고, 대표 PDF로 확인한다. [MinerU #5330](https://github.com/opendatalab/MinerU/issues/5330)
- 페이지 경계에서 표 행·열 대응이 깨지는 보고는 수치 존재보다 표 맥락 검사가 필요함을 뒷받침한다. [MinerU #5417](https://github.com/opendatalab/MinerU/issues/5417)
- 모델 슬러그의 공식 페이지는 확인했지만, 모든 공급자의 동작·비용·호환성을 이 리뷰에서 재검증한 것은 아니다. 가격·처리 시간·용량·몇 줄 구현이라는 수치는 가정으로 표기하고 실제 3편 처리 결과로 갱신한다. [OpenRouter 모델 페이지](https://openrouter.ai/deepseek/deepseek-v4.1-flash)
- `--no-images`는 내보낸 파일의 정책이다. 이미 이미지가 들어간 Git 이력과 LFS 객체를 없애지는 않는다. 추후 공개용 산출물은 허용 목록으로 별도 export하는 방식으로 명시한다. 이 리뷰는 소스별 라이선스의 법적 판단을 수행하지 않았다.

## 4. Overengineering을 피하는 Simple Plan

### 4.1 유지할 것 / 뒤로 미룰 것

| 유지 | 첫 버전에서 미룰 것 |
|---|---|
| Design Move + mutation/figure 참조 | 전체 kinase 범용 위치 매퍼 |
| SITE·GOAL·TACTIC과 짧은 별칭 표 | IDF·품질 배수·IFP/Tanimoto를 합친 랭킹 |
| kb/ref/dist 소유권 분리 | 광범위한 context 복제와 context_sha 체계 |
| 원문 위치와 효과 조건 | 자동 Figure 점수식·LLM fallback·100건 골드셋 |
| JSONL + manifest + CONTRACT | 별도 JS 참조 리더·모든 형태의 카드 |
| 하나의 Python 리더 | provider별 범용 adapter framework |
| 고정 snapshot과 실패 재개 | 전체 source 동기화·CIViC 대량 seed |
| 도메인 설정·스키마·프롬프트 분리 | 엔진 문자열 금지 테스트·doctor·14개 ADR |

엔진/도메인 분리라는 목표는 유지하되 일반 IO·검증·빌드 코드는 재사용하고, kinase 매핑·외부 source 처리는 도메인 코드로 둔다. **문자열이 없다는 검사보다, 일반 모듈이 kinase 코드에 의존하지 않는지가 중요하다.** 두 번째 도메인은 실제 요구가 생겼을 때 포크해서 검증한다.

### 4.2 권장 실행 순서

#### Step A — 실제 데이터 1건으로 계약 확정

- kinase 1개, 확보한 primary paper 1편, Move 1개, 관련 Figure 1개를 수동 작성한다.
- 고정 ID·효과 조건·근거 위치·검수 상태·구조 참조를 확정한다.
- Mutation이 필요한 사례라면 검수된 레코드 1개만 추가한다.
- **완료 기준:** observation/change/effect 각각을 원문에서 재확인할 수 있다.

#### Step B — 먼저 쓸 수 있는 파일 KB 만들기

- Move를 약 5개로 늘리고 최소 스키마·build·validate를 구현한다.
- dist는 우선 `manifest.json`, `CONTRACT.md`, `moves.jsonl`, `figures.jsonl`, 필요한 mutation/ref 파일만 만든다.
- Python 리더는 `get`, 필터 중심의 `find`, `cite`부터 구현한다. 그림은 ID 참조로 읽는다.
- **완료 기준:** 예제 질문 5개에 대해 관련 Move·근거·이미지 또는 이미지 없음 상태를 반환한다. 부정 질의와 결과 없음도 포함한다.

#### Step C — 자동화보다 먼저 재현성과 오류 처리

- 고정 source snapshot, 캐시 키, 실패 기록, 임시 빌드 후 교체를 넣는다.
- 지원 target에 한정된 위치 매핑과 반례 테스트를 추가한다.
- **완료 기준:** 같은 입력의 재실행은 동일 결과를 내고, 잘못된 입력 하나가 기존 KB를 손상시키지 않는다.

#### Step D — 병목이 된 작업만 자동화

- 실제로 확보한 PDF 3편에 MinerU를 적용해 표·각주·그림 연결을 확인한다.
- 한 모델·한 추출 함수로 staging 후보 생성, 자동 검사, 테마별 리뷰를 연결한다.
- **완료 기준:** 자동 후보를 사람이 승인해 기존 KB에 추가할 수 있고, 실패·거부 후보는 다시 구분된다.

#### Step E — 사용 결과에 따라 확장

- 수집이 병목이면 PubMed/CIViC 자동화, 검색이 병목이면 descriptor 유사도, 그림 검수가 병목이면 Figure 자동 선별을 추가한다.
- 다중 앱 지원은 우선 파일 계약으로 충족하고 실제 두 번째 소비자가 요구할 때 언어별 구현을 보강한다.

### 최종 의견

**Move 중심·3축 어휘·파일 계약은 유지하는 것을 권장한다.** 먼저 고칠 것은 ① 자동 검사와 사람 검수 분리, ② 수치의 실험 맥락과 주장별 근거, ③ 지원 범위가 명확한 위치 매핑, ④ 패널·chain·ligand 단위 구조 연결, ⑤ 고정 ID와 버전이다.

현재의 phase 순서는 범용 기반 구축을 끝내야 소비 가치를 확인할 수 있다. 이를 **수동 Move → 조회 가능한 dist → 오류 복구 → 추출 자동화**로 바꾸면, 더 적은 구현으로 이 KB가 실제 설계 agent에 유용한지 먼저 확인할 수 있다.
